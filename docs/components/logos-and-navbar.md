# Logos and Navbar

Operational navbar contract by theme.

## Requirements

- Left: product switch + ring/product lockup.
- Right: neutral icon actions + avatar + menu chevron.
- Keep consistent spacing and alignment across themes.
- Use theme-specific logo mapping; do not use placeholders.

## Baseline

- Design baseline: `1440px`.

Spec: `component-specs/navbar.json`.
